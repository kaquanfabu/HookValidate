name: build-dylib

on:
  push:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y clang make git perl unzip wget xz-utils

    - name: Install Theos
      run: |
        git clone https://github.com/theos/theos.git $HOME/theos

    - name: Install Theos toolchain
      run: |
        mkdir -p $HOME/theos/toolchain/linux/iphone
        cd $HOME/theos/toolchain/linux/iphone
        wget https://github.com/theos/toolchain/releases/latest/download/linux-ios-arm64-clang-toolchain.tar.xz
        tar xf linux-ios-arm64-clang-toolchain.tar.xz

    - name: Setup Theos
      run: |
        export THEOS=$HOME/theos
        $THEOS/bin/update-theos

    - name: Install iOS SDK
      run: |
        mkdir -p $HOME/theos/sdks
        cd $HOME/theos/sdks
        wget https://github.com/theos/sdks/archive/master.zip
        unzip master.zip
        mv sdks-master/* .
        rm -rf master.zip sdks-master

    - name: Build
      run: |
        export THEOS=$HOME/theos
        make

    - name: Upload dylib
      uses: actions/upload-artifact@v4
      with:
        name: dylib
        path: .theos/obj/debug/*.dylib
